<?xml version='1.0' encoding='UTF-8'?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">


	<!-- When deployed as Alfresco Share JAR, "share-config-custom.xml" local 
		file overrides {alfresco.home}/tomcat/shared/classes/alfresco/web-extension/share-config-custom.xml 
		So it's necessary to rename local file and load it onto ConfigBootstrap section -->
	<bean id="signDocumentAction.config" class="org.springframework.extensions.config.ConfigBootstrap"
		init-method="register">
		<property name="configService" ref="web.config" />
		<property name="configs">
			<list>
				<value>classpath:alfresco/web-extension/sign-action-config.xml</value>
			</list>
		</property>
	</bean>

	<bean id="signDocumentAction.resources"
		class="org.springframework.extensions.surf.util.ResourceBundleBootstrapComponent">
		<property name="resourceBundles">
			<list>
				<value>alfresco.messages.sign-action</value>
			</list>
		</property>
	</bean>

	<!-- Redefines timeout for Share webscript connections -->
	<bean id="connector.remoteclient" parent="connector.remoteclient.abstract"
		class="org.springframework.extensions.webscripts.connector.RemoteClient"
		scope="prototype">
		<!-- the http.connection.timeout value in milliseconds to apply to HTTP 
			connections -->
		<property name="connectTimeout">
			<value>30000</value>
		</property>
		<!-- the http.socket.timeout value in milliseconds to apply to HTTP connections -->
		<property name="readTimeout">
			<value>600000</value>
		</property>
	</bean>
	
</beans>